# tmux host badge aliases
# Format: raw_host|alias|fg|bg
# - raw_host: ssh destination token or local hostname
# - alias: displayed label (sanitized to [a-z0-9_.-])
# - fg/bg: tmux color names (example: colour232, colour45)
#
# Privacy note:
# - Do not add IP addresses. The tmux helper masks IP-based targets.
# - The local machine defaults to its hostname; keep extra aliases short and non-sensitive.

{{ $self := lower .chezmoi.hostname }}
{{ $self }}|{{ $self }}|colour232|colour45

# Example aliases:
# workstation|home|colour232|colour45
# server|cloud|colour231|colour160
# devbox|lab|colour232|colour111
