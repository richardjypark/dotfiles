- If more warm-apply speed is needed, focus on structural reductions in the two remaining always-run scripts (`run_after_38-setup-pi-maintenance-agent.sh.tmpl` and `run_after_99-performance-summary.sh`) without giving up self-healing or local opt-in behavior. Recent warm-path parse deferral and summary micro-cleanups helped a bit, so future work should look for larger behavior-preserving simplifications rather than smaller shell micro-opts.
- If the warm-apply loop still needs larger wins after the safe `run_onchange_` conversions, consider a planned dispatcher/consolidation pass for always-noop setup scripts to reduce shell-process overhead across `.chezmoiscripts/`; treat this as a plan-first change rather than an opportunistic tweak.
- Revisit the Neovim script semantics before tightening its `run_onchange_` state keying: the current script treats `REQUIRED_NVIM_VERSION` as the steady-state contract, while the pinned Neovim release acts more like a preferred install source than a strict must-match version.
- Review whether committed `private_dot_codex/private_config.toml.tmpl` should continue auto-setting `trust_level = "trusted"`, or at least document the rationale and expected local-override path. This looks like a potentially similar client-safety-default issue, but it needs Codex/Pi trust-model review before changing.
