- If more warm-apply speed is needed, evaluate whether the remaining always-run scripts (`run_after_38-setup-pi-maintenance-agent.sh.tmpl` and `run_after_99-performance-summary.sh`) can be optimized without giving up important self-healing or local opt-in behavior.
- If the warm-apply loop still needs larger wins after the safe `run_onchange_` conversions, consider a planned dispatcher/consolidation pass for always-noop setup scripts to reduce shell-process overhead across `.chezmoiscripts/`.
- Revisit the Neovim script semantics before tightening its `run_onchange_` state keying: the current script treats `REQUIRED_NVIM_VERSION` as the steady-state contract, while the pinned Neovim release acts more like a preferred install source than a strict must-match version.
