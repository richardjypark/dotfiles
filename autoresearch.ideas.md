- Low-hanging warm-path wins have now landed in both remaining always-run scripts (`run_after_38-setup-pi-maintenance-agent.sh.tmpl` and `run_after_99-performance-summary.sh`). If more warm-apply speed is needed, future work should look for larger behavior-preserving simplifications rather than more shell micro-opts.
- The older dispatcher/consolidation idea now looks mostly stale: only two always-run scripts remain in `.chezmoiscripts/`, and both have already received their low-hanging warm-path cleanups. Revisit broader shell-process consolidation only if fresh profiling shows a new bottleneck outside those scripts.
- Revisit the Neovim script semantics before tightening its `run_onchange_` state keying: the current script treats `REQUIRED_NVIM_VERSION` as the steady-state contract, while the pinned Neovim release acts more like a preferred install source than a strict must-match version.
- Revisit whether the remaining tracked repo-local Claude permissions can narrow any further after observing real workflow needs. The easy stale candidates (`WebFetch(domain:*)`, the stale Influx fetch domain, `Bash(dscl:*)`, `Bash(tree:*)`, `Bash(wc:*)`, `Bash(cat:*)`, `Bash(alias:*)`, `Bash(czu:*)`, `Bash(chmod:*)`, `Bash(mkdir:*)`, `Bash(source:*)`) are now gone; the remaining rules (`chezmoi`, `jj`, `git`, `zsh`, `tmux`) look much more like true workflow primitives, so future tightening needs materially stronger evidence.
