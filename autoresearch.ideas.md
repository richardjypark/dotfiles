- Low-hanging warm-path wins have now landed in both remaining always-run scripts (`run_after_38-setup-pi-maintenance-agent.sh.tmpl` and `run_after_99-performance-summary.sh`). If more warm-apply speed is needed, future work should look for larger behavior-preserving simplifications rather than more shell micro-opts.
- If the warm-apply loop still needs larger wins after the safe `run_onchange_` conversions, consider a planned dispatcher/consolidation pass for always-noop setup scripts to reduce shell-process overhead across `.chezmoiscripts/`; treat this as a plan-first change rather than an opportunistic tweak.
- Revisit the Neovim script semantics before tightening its `run_onchange_` state keying: the current script treats `REQUIRED_NVIM_VERSION` as the steady-state contract, while the pinned Neovim release acts more like a preferred install source than a strict must-match version.
- Revisit whether the remaining tracked repo-local Claude Bash permissions can narrow any further after observing real workflow needs. The easy stale candidates (`WebFetch(domain:*)`, `Bash(dscl:*)`, `Bash(tree:*)`, `Bash(wc:*)`, `Bash(cat:*)`, `Bash(alias:*)`) are now gone; the remaining rules (`chezmoi`, `jj`, `git`, `zsh`, `tmux`, `mkdir`, `chmod`, `source`, `czu`) have clearer workflow grounding, so future tightening needs materially stronger evidence.
