- Low-hanging warm-path wins have now landed in both remaining always-run scripts (`run_after_38-setup-pi-maintenance-agent.sh.tmpl` and `run_after_99-performance-summary.sh`). A combined warm-path benchmark now puts their residual cost at roughly 5.6 ms per apply in the current harness, so future speed work should look for larger behavior-preserving simplifications rather than more shell micro-opts.
- The low-risk helper-command/state-guidance/doc-symmetry cleanup backlog now looks mostly exhausted: `chezmoi-rerun-script` is surfaced across user + maintainer docs and the apply summary, `CLAUDE.md` now distinguishes targeted reruns from full reruns, stale pre-`run_onchange_*` references were cleaned up, and `czm` is now named alongside `czl` in the remaining quick-skim helper docs. The lightweight validation follow-through is also largely done: `chezmoi-health-check` now covers the rerun helper, and CI now syntax-checks the full top-level `dot_local/bin/` shell entrypoint set. Future docs/validation work should come from a concrete inconsistency or integration gap, not from assuming more low-hanging command-list or syntax-coverage gaps exist.
- The Neovim script semantics are now commented inline: `REQUIRED_NVIM_VERSION` is the compatibility floor and the pinned release is only the preferred install source. Any future tightening of its `run_onchange_` state keying should still be treated as a deeper behavior change, not another low-risk doc/comment pass.
- Revisit whether the remaining tracked repo-local Claude permissions can narrow any further after observing real workflow needs. The easy stale candidates (`WebFetch(domain:*)`, the stale Influx fetch domain, `Bash(dscl:*)`, `Bash(tree:*)`, `Bash(wc:*)`, `Bash(cat:*)`, `Bash(alias:*)`, `Bash(czu:*)`, `Bash(chmod:*)`, `Bash(mkdir:*)`, `Bash(source:*)`) are now gone; the remaining rules (`chezmoi`, `jj`, `git`, `zsh`, `tmux`) look much more like true workflow primitives, so future tightening needs materially stronger evidence.
